export default class Client {
  constructor({ documento, nombreCompleto, NumeroDeCuenta, saldo}) {
   this.documento = documento,
   this.nombreCompleto = nombreCompleto,
   this.numeroDeCuenta = numeroDeCuenta,
   this.saldo = saldo
  }
}

